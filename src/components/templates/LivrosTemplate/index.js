import React from 'react'
import styled from 'styled-components'
import { Layout } from 'components'

const StyledLayout = styled(Layout)`
  height: 100vh;
`

const LivrosTemplate = () => {
  return (
    <StyledLayout>
    </StyledLayout>
  )
}

LivrosTemplate.propTypes = {}

export default LivrosTemplate
